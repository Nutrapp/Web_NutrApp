<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap"
        rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet"
        crossorigin="anonymous" />
    <link rel="stylesheet" href="css/style.css" />
    <title>RotuScan - Adicionar Ingredientes</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet" />
    <link href="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/aos.css" rel="stylesheet" />
    
    <style>
        /* Fundo verde e padding para a navbar fixa */
        body {
            background-color: #00A13E; /* Seu verde RotuScan */
            padding-top: 100px; 
            min-height: 100vh;
        }

        /* Container Principal que centraliza tudo (usando flexbox do Bootstrap) */
        .main-content-wrapper {
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 20px;
        }

        /* Título */
        .page-title {
            color: white;
            font-size: clamp(1.8rem, 5vw, 2.5rem); /* Título responsivo */
            margin-bottom: 20px;
            font-weight: 600;
        }

        /* Card Branco Centralizado e Responsivo */
        .add-ingredientes-card {
            background-color: white;
            border-radius: 15px;
            padding: clamp(20px, 5vw, 40px); /* Padding responsivo */
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
            max-width: 900px; /* Limite de largura para desktop */
            width: 100%;
        }

        /* Estilo da área de seleção de ingredientes */
        .ingredientes-grid {
            max-height: 400px; /* Limite de altura para scroll */
            overflow-y: auto;
            border: 1px solid #e0e0e0;
            border-radius: 8px;
            padding: 15px;
            margin-top: 10px;
        }

        /* Responsividade para a lista de checkboxes */
        .ingredientes-grid .col {
            margin-bottom: 0.5rem; /* Pequeno espaçamento vertical */
        }
        
        /* Cores dos botões customizadas */
        .btn-success-custom {
            background-color: #00A13E;
            border-color: #00A13E;
            font-weight: 600;
            transition: background-color 0.3s;
        }
        .btn-success-custom:hover {
            background-color: #007F31;
            border-color: #007F31;
        }
    </style>
</head>

<body>
    
     <div class="loading-screen" id="loadingScreen">
    <div class="loading-spinner"></div>
  </div>

  <div class="scroll-progress" id="scrollProgress"></div>

  <nav class="navbar navbar-expand-lg fixed-top" id="navbar">
    <div class="container">
      <a href="#home">
        <img src="imgs/logos/RotoScan.png" class="iconLogo" />
      </a>
      <br>
      <br>
      <div class="logo-text">
        <span>Rotu</span><span>Scan</span>
      </div>


      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
      </div>
    </div>
  </nav>

    <div class="main-content-wrapper">
        
        <h1 class="page-title">Adicionar Ingredientes</h1>

        <div class="add-ingredientes-card">
            
            <form action="/ingredientes/salvar" method="POST">
                
                <input type="hidden" name="produtoId" value="<%= produtoId %>">

                <div class="mb-4">
                    <h5 class="text-muted mb-3">Selecione os ingredientes presentes no produto:</h5>
                    
                    <div class="ingredientes-grid">
                        
                        <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 row-cols-lg-4 g-3"> 
                            
                            <% if (ingredientes && ingredientes.length > 0) { %>
                                <% ingredientes.forEach(ingrediente => { %>
                                    <div class="col">
                                        <div class="form-check">
                                            <input 
                                                class="form-check-input" 
                                                type="checkbox" 
                                                name="ingredientesSelecionados" 
                                                value="<%= ingrediente.id %>" 
                                                id="ingrediente_<%= ingrediente.id %>"
                                            >
                                            <label class="form-check-label" for="ingrediente_<%= ingrediente.id %>">
                                                <%= ingrediente.nome %>
                                            </label>
                                        </div>
                                    </div>
                                <% }); %>
                            <% } else { %>
                                <div class="col-12">
                                    <p class="alert alert-info text-center">Nenhum ingrediente disponível para seleção. Cadastre alguns!</p>
                                </div>
                            <% } %>

                        </div> </div> </div>

                <div class="d-flex flex-column flex-sm-row justify-content-end gap-2 pt-3 border-top">
                    <a href="/produtos" class="btn btn-secondary w-100 w-sm-auto">Voltar sem Salvar</a>
                    <button type="submit" class="btn btn-success-custom w-100 w-sm-auto">Finalizar Cadastro!</button>
                </div>

            </form>
            
        </div> </div> <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"
        crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/aos.js"></script>
    <script src="/js/script.js"></script>

</body>

</html>